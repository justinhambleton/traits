# Test fixture: Child profile extending parent (chain level 3)
schema: "v1.6"
extends: "_extends-chain-parent"

meta:
  name: "_extends-chain-child"
  version: "1.0.0"
  description: "Leaf child in 3-level chain"
  tags:
    - "chain-child"

identity:
  backstory: "Child backstory overrides parent"

voice:
  directness: "high"

vocabulary:
  preferred_terms:
    - "child-term"
  forbidden_terms:
    - "child-forbidden"

behavioral_rules:
  - "Child rule added"
